<Window x:Class="DS.WpfApp.Licensing.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DS.WpfApp.Licensing" 
        xmlns:viewmodel="clr-namespace:DS.WpfApp.Licensing.ViewModel" 
        d:DataContext="{d:DesignInstance Type=viewmodel:MainWindowViewModel}"
        mc:Ignorable="d"
        ResizeMode="NoResize" Topmost="True"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="200" Width="300">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/StyleDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Button Content="Button" Style="{StaticResource DS_Button}"
                Command="{Binding Start}"
                HorizontalAlignment="Center" 
                VerticalAlignment="Top" Width="153" Margin="0,38,0,0"/>
        <TextBox HorizontalAlignment="Center" 
                 TextWrapping="Wrap" 
                 Text="{Binding Path=Text}" 
                 VerticalAlignment="Top" Width="120" Margin="0,94,0,0"/>

    </Grid>
</Window>
